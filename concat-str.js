//Create a concatStr function that takes 2 arguments and concatenate them

const one = 2
const two = 4
const concated = ''
const concatStr = (a, b) => concated.concat(a, b); 

//console.log(concatStr(one, two))