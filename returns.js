/*
id that takes one argument and return it
getLength that takes an array or a string and return its length
*/
const arr = [0,2,3,4,5]

const id = (n) => n
const getLength = (m) => m.length

//console.log(getLength(arr))